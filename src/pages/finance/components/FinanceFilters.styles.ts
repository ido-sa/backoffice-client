import { SxProps, Theme } from '@mui/material'

export const FinanceFiltersStyles = {
  container: {
    mb: 3,
    p: 2,
    border: '1px solid',
    borderColor: 'divider',
    borderRadius: 1,
    backgroundColor: 'background.paper',
  } as SxProps<Theme>,

  applyButton: {
    height: '40px',
    fontWeight: 600,
  } as SxProps<Theme>,
}
